package-as: AutoItemMacro

ignore:
  - .github
  - README.md
  - Tools
  # The CurseForge project avatar, uploaded on the website rather than shipped.
  # Media/Logo.tga is the one the addon actually draws.
  - Media/Avatar.png
